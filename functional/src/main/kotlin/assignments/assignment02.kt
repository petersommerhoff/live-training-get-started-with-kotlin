package assignments

/**
 * @author Peter Sommerhoff <peter@petersommerhoff.com>
 */

// Exercise: create the following extensions:
// 1) an extension property BD on Int that returns a BigDecimal representation of the Int
// - this allows writing 17.BD, similar to 17L for Long
// 2) an extension function copy() on MutableList<Int> that returns a copy of the mutable receiver list (generics later)
